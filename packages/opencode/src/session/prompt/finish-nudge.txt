[WARNING] The turn ended without a successful finish call.

Call `finish` to complete the turn. If the current work changed files, first run a synchronous `review` task with `subagent_type: "review"` and `background: false`. If the latest review returned `Needs fixes`, fix the findings and review those fixes again; the previous review no longer applies.

After an explicit approval, call `finish` exactly once with a concise result. If the review cap is reached, report `review-cap` rather than claiming approval.

(This is an automated reminder. Do not answer it conversationally.)
